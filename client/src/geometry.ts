export interface Point {
  x: number;
  y: number;
}

export interface SideFlags {
  bottom: boolean;
  left: boolean;
  right: boolean;
  top: boolean;
}

export interface Size {
  width: number;
  height: number;
}
